# --- Databáze (Supabase Postgres) -------------------------------------------
# Aplikace jede přes transaction pooler (port 6543) — kvůli serverless funkcím.
DATABASE_URL="postgresql://postgres.<ref>:<heslo>@aws-0-eu-central-1.pooler.supabase.com:6543/postgres"
# drizzle-kit push potřebuje session connection (port 5432), pooler mu nestačí.
DIRECT_URL="postgresql://postgres.<ref>:<heslo>@aws-0-eu-central-1.pooler.supabase.com:5432/postgres"

# --- Admin ------------------------------------------------------------------
# HTTP Basic heslo pro /admin (zadání seznamu lidí, reset hry).
# Když není nastavené, /admin je v produkci zavřený úplně.
ADMIN_PASSWORD="vanoce2026"
